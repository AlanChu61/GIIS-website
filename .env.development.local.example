# Optional override (gitignored). The repo includes `.env.development` with the same default.
# Use this file if your transcript API runs on a different host or port.
REACT_APP_API_URL=http://localhost:4000
